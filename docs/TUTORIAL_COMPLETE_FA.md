<div dir="rtl" align="right">

# 📚 راهنمای کامل نرم‌افزار StructureMaster
## 🎯 مخصوص علاقه‌مندان به برنامه‌نویسی

---

<div align="center">

### 🌟 به دنیای جادویی مدیریت پروژه‌ها خوش آمدی! 🌟

**نوشته شده برای جوانان کنجکاو و علاقه‌مند به برنامه‌نویسی**

نسخه: ۱.۰.۰ | تاریخ: آذر ۱۴۰۳

</div>

---

## 📋 فهرست مطالب

<div dir="rtl">

| شماره | عنوان | صفحه |
|-----:|------|------|
| ۱ | [مقدمه: این برنامه چیست؟](#-بخش-اول-مقدمه--این-برنامه-چیست) | ۱ |
| ۲ | [پیش‌نیازها: چه چیزهایی لازم داریم؟](#-بخش-دوم-پیشنیازها--چه-چیزهایی-لازم-داریم) | ۲ |
| ۳ | [نصب برنامه](#-بخش-سوم-نصب-برنامه) | ۳ |
| ۴ | [ساختار پروژه و فایل‌ها](#-بخش-چهارم-ساختار-پروژه-و-فایلها) | ۴ |
| ۵ | [ماژول‌های اصلی برنامه](#-بخش-پنجم-ماژولهای-اصلی-برنامه) | ۵ |
| ۶ | [رابط خط فرمان (CLI)](#-بخش-ششم-رابط-خط-فرمان-cli) | ۶ |
| ۷ | [رابط گرافیکی (GUI)](#-بخش-هفتم-رابط-گرافیکی-gui) | ۷ |
| ۸ | [انواع پروژه‌های قابل شناسایی](#-بخش-هشتم-انواع-پروژههای-قابل-شناسایی) | ۸ |
| ۹ | [ارتباط بین بخش‌ها](#-بخش-نهم-ارتباط-بین-بخشها) | ۹ |
| ۱۰ | [نکات و ترفندها](#-بخش-دهم-نکات-و-ترفندها) | ۱۰ |

</div>

---

# 🎮 بخش اول: مقدمه — این برنامه چیست؟

## 🤔 تصور کن که...

فرض کن یک روز تصمیم می‌گیری یک بازی کامپیوتری بسازی. برای این کار باید فایل‌های زیادی درست کنی:
- فایل‌هایی برای کدهای بازی
- فایل‌هایی برای تصاویر
- فایل‌هایی برای صداها
- فایل‌هایی برای ذخیره امتیازات

اگر همه این فایل‌ها را توی یک پوشه بریزی، خیلی شلوغ و گیج‌کننده می‌شود! 😵

## 💡 راه‌حل چیست؟

برنامه‌نویس‌های حرفه‌ای فایل‌هایشان را **دسته‌بندی** می‌کنند. مثلاً:

</div>

```
my-game/
├── 📁 src/                 ← کدهای اصلی بازی
│   ├── 📄 main.py          ← فایل شروع بازی
│   ├── 📄 player.py        ← کد مربوط به بازیکن
│   └── 📄 enemy.py         ← کد مربوط به دشمنان
├── 📁 assets/              ← فایل‌های کمکی
│   ├── 📁 images/          ← تصاویر
│   └── 📁 sounds/          ← صداها
├── 📁 tests/               ← تست‌ها
└── 📄 README.md            ← توضیحات پروژه
```

<div dir="rtl">

## 🌟 StructureMaster چه کاری انجام می‌دهد؟

این برنامه **سه کار اصلی** انجام می‌دهد:

### 🔨 کار اول: ساختن پروژه
اگر ساختار یک پروژه را به برنامه بدهی (مثلاً به صورت متن یا فایل JSON)، برنامه **تمام پوشه‌ها و فایل‌ها را می‌سازد**!

### 🔍 کار دوم: استخراج ساختار
اگر یک پروژه موجود داشته باشی، برنامه می‌تواند **ساختارش را استخراج کند** و به تو نشان دهد.

### 📄 کار سوم: استخراج محتوا
برنامه می‌تواند **محتوای تمام فایل‌ها** را همراه با اطلاعات اضافی (مثل تاریخ ایجاد، اندازه، نوع فایل) استخراج کند.

---

# 🛠 بخش دوم: پیش‌نیازها — چه چیزهایی لازم داریم؟

## 🐍 پایتون (Python)

### پایتون چیست؟
پایتون یک **زبان برنامه‌نویسی** است. مثل زبان فارسی که ما باهاش حرف می‌زنیم، پایتون زبانی است که با کامپیوتر حرف می‌زنیم!

### چرا پایتون؟
- **ساده است**: مثل نوشتن انگلیسی ساده
- **قدرتمند است**: می‌توان هر کاری باهاش کرد
- **محبوب است**: خیلی‌ها ازش استفاده می‌کنند

### نصب پایتون

۱. به سایت [python.org](https://python.org) برو
۲. روی دکمه "Download Python" کلیک کن
۳. فایل را اجرا کن و نصب کن

> ⚠️ **نکته مهم**: موقع نصب، حتماً گزینه "Add Python to PATH" را تیک بزن!

### بررسی نصب
بعد از نصب، این دستور را در **Command Prompt** بزن:

</div>

```bash
python --version
```

<div dir="rtl">

اگر چیزی مثل `Python 3.11.5` نشان داد، یعنی درست نصب شده! ✅

---

## 📦 pip — مدیر بسته‌های پایتون

### pip چیست؟
pip مثل یک **فروشگاه برنامه** است! وقتی می‌خواهی یک کتابخانه جدید نصب کنی، از pip استفاده می‌کنی.

### مثال استفاده

</div>

```bash
# نصب یک کتابخانه
pip install click

# نصب چند کتابخانه از فایل
pip install -r requirements.txt
```

<div dir="rtl">

---

## 📚 کتابخانه‌های مورد نیاز

### کتابخانه چیست؟
کتابخانه مجموعه‌ای از کدهای آماده است که کسی قبلاً نوشته. به جای اینکه همه چیز را از صفر بنویسی، از کتابخانه‌ها استفاده می‌کنی!

### کتابخانه‌های اصلی این پروژه

| نام | کاربرد | توضیح ساده |
|-----|--------|------------|
| **PyQt6** | رابط گرافیکی | برای ساختن پنجره و دکمه |
| **click** | خط فرمان | برای ساختن دستورات |
| **rich** | خروجی زیبا | برای رنگی کردن متن‌ها |
| **cryptography** | رمزنگاری | برای قفل کردن فایل‌ها |
| **PyYAML** | فایل YAML | برای خواندن فایل‌های تنظیمات |
| **chardet** | تشخیص کدگذاری | برای فهمیدن نوع فایل |
| **FastAPI** | وب API | برای ساختن سرور وب |

---

# 💿 بخش سوم: نصب برنامه

## مرحله ۱: دانلود پروژه

</div>

```bash
git clone https://github.com/your-repo/structure-master.git
cd structure-master
```

<div dir="rtl">

> 💡 **یادآوری**: `git` برنامه‌ای است برای دانلود پروژه‌ها از اینترنت.

## مرحله ۲: نصب کتابخانه‌ها

</div>

```bash
pip install -r requirements.txt
```

<div dir="rtl">

این دستور همه کتابخانه‌های لازم را نصب می‌کند.

## مرحله ۳: تست برنامه

### اجرای CLI (خط فرمان):

</div>

```bash
python -m src.cli.cli --help
```

<div dir="rtl">

### اجرای GUI (رابط گرافیکی):

</div>

```bash
python -m src.main --gui
```

<div dir="rtl">

---

# 📂 بخش چهارم: ساختار پروژه و فایل‌ها

## 🗺️ نقشه پروژه

</div>

```
structure-creator-optus/
│
├── 📁 src/                          ← کدهای اصلی برنامه
│   │
│   ├── 📄 __init__.py               ← فایل شناسایی پکیج
│   ├── 📄 config.py                 ← تنظیمات برنامه
│   ├── 📄 utils.py                  ← توابع کمکی
│   ├── 📄 main.py                   ← نقطه شروع برنامه
│   │
│   ├── 📁 modules/                  ← ماژول‌های اصلی
│   │   ├── 📄 logger.py             ← سیستم لاگ (ثبت رویدادها)
│   │   ├── 📄 parser.py             ← تجزیه‌گر ساختار
│   │   ├── 📄 validator.py          ← اعتبارسنجی
│   │   ├── 📄 scanner.py            ← اسکنر پروژه
│   │   ├── 📄 builder.py            ← سازنده ساختار
│   │   ├── 📄 exporter.py           ← صادرکننده
│   │   ├── 📄 content_extractor.py  ← استخراج محتوا
│   │   ├── 📄 security.py           ← امنیت و رمزنگاری
│   │   ├── 📄 profile_manager.py    ← مدیریت پروفایل
│   │   ├── 📄 diff_compare.py       ← مقایسه ساختارها
│   │   ├── 📄 cache_manager.py      ← مدیریت کش
│   │   ├── 📄 file_analyzer.py      ← تحلیل فایل
│   │   ├── 📄 project_detector.py   ← تشخیص نوع پروژه
│   │   └── 📄 plugin_manager.py     ← مدیریت افزونه
│   │
│   ├── 📁 cli/                      ← رابط خط فرمان
│   │   └── 📄 cli.py                ← دستورات CLI
│   │
│   ├── 📁 gui/                      ← رابط گرافیکی
│   │   ├── 📄 main_window.py        ← پنجره اصلی
│   │   ├── 📄 styles.py             ← استایل‌ها
│   │   ├── 📄 components.py         ← اجزای UI
│   │   └── 📁 tabs/                 ← تب‌های مختلف
│   │
│   ├── 📁 cloud/                    ← ابری و همگام‌سازی
│   │   ├── 📄 git_integration.py    ← کار با Git
│   │   └── 📄 github_sync.py        ← همگام با GitHub
│   │
│   ├── 📁 analytics/                ← تحلیل و نمودار
│   │   ├── 📄 statistics.py         ← آمار
│   │   └── 📄 charts.py             ← نمودارها
│   │
│   ├── 📁 search/                   ← جستجو
│   │   └── 📄 search_engine.py      ← موتور جستجو
│   │
│   ├── 📁 web/                      ← وب API
│   │   └── 📄 api.py                ← REST API
│   │
│   └── 📁 autodoc/                  ← مستندسازی خودکار
│       └── 📄 doc_generator.py      ← تولید مستندات
│
├── 📁 tests/                        ← تست‌ها
├── 📁 docs/                         ← مستندات
├── 📁 plugins/                      ← افزونه‌ها
├── 📁 templates/                    ← قالب‌ها
├── 📄 requirements.txt              ← لیست کتابخانه‌ها
├── 📄 setup.py                      ← فایل نصب
└── 📄 README.md                     ← توضیحات پروژه
```

<div dir="rtl">

## 📝 انواع فایل‌ها

### فایل‌های پایتون (`.py`)
این فایل‌ها کدهای برنامه هستند. پایتون این فایل‌ها را می‌خواند و اجرا می‌کند.

### فایل `__init__.py`
این فایل به پایتون می‌گوید که این پوشه یک **پکیج** است. پکیج مثل یک جعبه است که چند فایل مرتبط را نگه می‌دارد.

### فایل `requirements.txt`
لیست تمام کتابخانه‌هایی که برنامه به آن‌ها نیاز دارد.

### فایل `README.md`
توضیحات پروژه به زبان **Markdown**. این فایل معمولاً در صفحه GitHub نشان داده می‌شود.

### فایل `setup.py`
برای نصب برنامه به عنوان یک پکیج پایتون استفاده می‌شود.

---

# 🧩 بخش پنجم: ماژول‌های اصلی برنامه

## ماژول چیست؟

ماژول یک فایل پایتون است که کدهای مرتبط را نگه می‌دارد. مثلاً ماژول `scanner.py` همه کدهای مربوط به اسکن پروژه را دارد.

---

## 📋 ماژول Logger (ثبت‌کننده)

### وظیفه:
ثبت اتفاقات برنامه مثل یک دفتر خاطرات!

### چرا مهم است؟
وقتی برنامه مشکل پیدا می‌کند، می‌توانی ببینی چه اتفاقی افتاده.

### سطوح لاگ:

| سطح | رنگ | توضیح |
|-----|-----|-------|
| **TRACE** | ⚪ خاکستری | جزئی‌ترین اطلاعات |
| **DEBUG** | 🔵 آبی | برای برنامه‌نویس‌ها |
| **INFO** | 🟢 سبز | اطلاعات عادی |
| **WARN** | 🟡 زرد | هشدار |
| **ERROR** | 🔴 قرمز | خطا |
| **CRITICAL** | 🟣 بنفش | خطای خیلی مهم |

### مثال استفاده:

</div>

```python
from src.modules.logger import Logger

log = Logger()
log.info("برنامه شروع شد")
log.warn("حافظه کم است!")
log.error("فایل پیدا نشد")
```

<div dir="rtl">

---

## 📖 ماژول Parser (تجزیه‌گر)

### وظیفه:
فهمیدن ساختار پروژه از متن یا فایل

### فرمت‌های قابل خواندن:

#### ۱. فرمت JSON:

</div>

```json
{
  "src": {
    "main.py": null,
    "utils.py": null
  },
  "tests": {}
}
```

<div dir="rtl">

#### ۲. فرمت درختی:

</div>

```
├── src/
│   ├── main.py
│   └── utils.py
└── tests/
```

<div dir="rtl">

#### ۳. فرمت مسیرها:

</div>

```
src/main.py
src/utils.py
tests/
```

<div dir="rtl">

### مثال استفاده:

</div>

```python
from src.modules.parser import StructureParser

parser = StructureParser()
result = parser.parse(text_input)

if result.success:
    print("ساختار درست بود!")
    print(result.structure)
```

<div dir="rtl">

---

## ✅ ماژول Validator (اعتبارسنج)

### وظیفه:
بررسی اینکه ساختار درست است یا نه

### چه چیزهایی را بررسی می‌کند؟

| بررسی | توضیح |
|-------|-------|
| **نام فایل** | آیا کاراکترهای غیرمجاز دارد؟ |
| **نام‌های رزرو** | مثل CON, NUL در ویندوز |
| **طول مسیر** | آیا خیلی طولانی است؟ |
| **تکرار** | آیا دو فایل با یک نام هست؟ |
| **عمق** | آیا پوشه‌ها خیلی تودرتو هستند؟ |

### مثال خطاها:

</div>

```
❌ "file<name>.txt"   → کاراکتر < غیرمجاز است
❌ "CON.txt"          → نام CON رزرو شده است
❌ "a/b/c/d/e/f/..."  → عمق زیاد است
```

<div dir="rtl">

---

## 🔍 ماژول Scanner (اسکنر)

### وظیفه:
گشتن در یک پروژه و پیدا کردن تمام فایل‌ها و پوشه‌ها

### ویژگی‌ها:
- 🚀 **سریع**: از چند هسته پردازنده استفاده می‌کند
- 💾 **کش**: نتیجه را ذخیره می‌کند برای دفعات بعد
- 🎯 **هوشمند**: فایل‌های غیرلازم را نادیده می‌گیرد

### فایل‌هایی که نادیده گرفته می‌شوند:
- `node_modules/` — بسته‌های Node.js
- `__pycache__/` — کش پایتون
- `.git/` — فایل‌های Git
- `venv/` — محیط مجازی پایتون

### مثال استفاده:

</div>

```python
from src.modules.scanner import ProjectScanner

scanner = ProjectScanner()
result = scanner.scan("/path/to/project")

print(f"تعداد فایل‌ها: {result.stats['total_files']}")
print(f"اندازه کل: {result.stats['total_size']} بایت")
```

<div dir="rtl">

---

## 🔨 ماژول Builder (سازنده)

### وظیفه:
ساختن واقعی فایل‌ها و پوشه‌ها روی دیسک

### حالت‌های کار:

| حالت | توضیح |
|------|-------|
| **عادی** | فایل‌ها را می‌سازد |
| **force** | حتی اگر موجود باشد، بازنویسی می‌کند |
| **dry-run** | فقط نشان می‌دهد، چیزی نمی‌سازد |

### ویژگی rollback:
اگر وسط کار خطایی بدهد، همه تغییرات را برمی‌گرداند!

### مثال:

</div>

```python
from src.modules.builder import StructureBuilder

builder = StructureBuilder()

# حالت dry-run (فقط نشان دادن)
result = builder.build(structure, output_path, dry_run=True)
print("این فایل‌ها ساخته خواهند شد:")
for op in result.operations:
    print(f"  {op.path}")

# ساخت واقعی
result = builder.build(structure, output_path)
print(f"تعداد فایل‌های ساخته شده: {result.stats['files_created']}")
```

<div dir="rtl">

---

## 📤 ماژول Exporter (صادرکننده)

### وظیفه:
صادر کردن ساختار به فرمت‌های مختلف

### فرمت‌های خروجی:

| فرمت | پسوند | کاربرد |
|------|-------|--------|
| **JSON** | `.json` | برای برنامه‌ها |
| **TXT** | `.txt` | متن ساده درختی |
| **Markdown** | `.md` | مستندات زیبا |
| **YAML** | `.yaml` | فایل تنظیمات |
| **HTML** | `.html` | نمایش در مرورگر |
| **ZIP** | `.zip` | فشرده‌سازی |
| **TAR.GZ** | `.tar.gz` | فشرده لینوکسی |

### مثال:

</div>

```python
from src.modules.exporter import Exporter
from src.config import ExportFormat

exporter = Exporter()

# صادر کردن به JSON
exporter.export_structure(structure, "output.json", ExportFormat.JSON)

# صادر کردن به HTML
exporter.export_structure(structure, "output.html", ExportFormat.HTML)
```

<div dir="rtl">

---

## 📄 ماژول ContentExtractor (استخراج محتوا)

### وظیفه:
خواندن محتوای فایل‌ها همراه با اطلاعات اضافی

### اطلاعاتی که استخراج می‌شود:

| اطلاعات | توضیح |
|---------|-------|
| **محتوا** | متن داخل فایل |
| **اندازه** | چند بایت است |
| **هش SHA-256** | اثر انگشت فایل |
| **نوع MIME** | مثلاً `text/plain` |
| **کدگذاری** | مثلاً UTF-8 |
| **تاریخ ایجاد** | کی ساخته شده |
| **تاریخ تغییر** | آخرین تغییر |

### هش چیست؟
هش مثل **اثر انگشت** فایل است. حتی با کوچک‌ترین تغییر، هش عوض می‌شود!

### مثال:

</div>

```python
from src.modules.content_extractor import ContentExtractor

extractor = ContentExtractor()
result = extractor.extract(files_list)

for file in result.files:
    print(f"فایل: {file.filename}")
    print(f"اندازه: {file.size} بایت")
    print(f"هش: {file.sha256_hash[:20]}...")
```

<div dir="rtl">

---

## 🔐 ماژول Security (امنیت)

### وظیفه:
محافظت از فایل‌ها با رمزنگاری

### رمزنگاری AES-256 چیست؟
یک روش بسیار قوی برای قفل کردن اطلاعات. حتی قوی‌ترین کامپیوترها هم میلیاردها سال طول می‌کشد تا بشکنند!

### کاربردها:
- 🔒 رمزنگاری خروجی‌ها
- 🔍 تشخیص اطلاعات حساس (رمزها، کلیدها)
- 🧹 پاکسازی اطلاعات حساس

### مثال:

</div>

```python
from src.modules.security import SecurityManager

security = SecurityManager()

# رمزنگاری
encrypted = security.encrypt("متن مخفی", "رمزعبور123")

# رمزگشایی
decrypted = security.decrypt(encrypted, "رمزعبور123")
```

<div dir="rtl">

---

## 📊 ماژول ProjectDetector (تشخیص پروژه)

### وظیفه:
فهمیدن نوع پروژه (لاراول؟ ری‌اکت؟ پایتون؟)

### چگونه کار می‌کند؟
به فایل‌های خاص نگاه می‌کند:

| اگر این فایل باشد... | پروژه احتمالاً... |
|----------------------|-------------------|
| `artisan` | Laravel (لاراول) |
| `package.json` + `src/App.jsx` | React (ری‌اکت) |
| `manage.py` | Django (جنگو) |
| `go.mod` | Go (گو) |
| `Cargo.toml` | Rust (راست) |
| `setup.py` | Python (پایتون) |

---

# 💻 بخش ششم: رابط خط فرمان (CLI)

## CLI چیست؟

CLI یعنی **Command Line Interface** — یعنی کار کردن با برنامه از طریق تایپ دستور.

## دستورات اصلی

### ۱. دستور scan (اسکن)

</div>

```bash
# اسکن یک پروژه
python -m src.cli.cli scan /path/to/project

# ذخیره خروجی در فایل
python -m src.cli.cli scan /path/to/project -o output.json -f json
```

<div dir="rtl">

### ۲. دستور build (ساخت)

</div>

```bash
# ساخت از فایل JSON
python -m src.cli.cli build structure.json -o ./my-project

# پیش‌نمایش (بدون ساخت واقعی)
python -m src.cli.cli build structure.json -o ./my-project --dry-run
```

<div dir="rtl">

### ۳. دستور extract (استخراج)

</div>

```bash
# استخراج با محتوا
python -m src.cli.cli extract /project -o output.txt --content

# استخراج با رمزنگاری
python -m src.cli.cli extract /project -o output.enc --encrypt -p mypassword
```

<div dir="rtl">

### ۴. دستور compare (مقایسه)

</div>

```bash
# مقایسه دو پروژه
python -m src.cli.cli compare /old-project /new-project -o diff.md
```

<div dir="rtl">

### ۵. دستور analyze (تحلیل)

</div>

```bash
# تحلیل کیفیت کد
python -m src.cli.cli analyze /project -o analysis.json
```

<div dir="rtl">

---

# 🖥 بخش هفتم: رابط گرافیکی (GUI)

## GUI چیست؟

GUI یعنی **Graphical User Interface** — یعنی پنجره و دکمه و منو!

## اجرای GUI

</div>

```bash
python -m src.main --gui
```

<div dir="rtl">

## تب‌های موجود

### 🔨 تب Structure Builder
- وارد کردن ساختار به صورت متنی
- خواندن از کلیپ‌بورد
- بارگذاری از فایل
- پیش‌نمایش ساختار
- ساخت پروژه

### 📂 تب Extract Structure
- انتخاب پروژه
- اسکن و نمایش ساختار
- صادر کردن به فرمت‌های مختلف

### 📄 تب Content Extractor
- استخراج محتوای فایل‌ها
- نمایش متادیتا
- رمزنگاری خروجی

### 🔍 تب Search
- جستجوی regex
- جستجو در محتوا
- پیدا کردن TODO/FIXME

### 📊 تب Analytics
- نمودار دایره‌ای نوع فایل‌ها
- نمودار اندازه فایل‌ها
- آمار کلی

### ⚙️ تب Settings
- تنظیمات برنامه
- مدیریت پروفایل‌ها

---

# 🎯 بخش هشتم: انواع پروژه‌های قابل شناسایی

## زبان‌ها و فریمورک‌ها

### 🐍 پایتون (Python)

| نوع | فایل شاخص |
|-----|-----------|
| پایتون ساده | `setup.py`, `pyproject.toml` |
| Django | `manage.py`, `settings.py` |
| Flask | `app.py`, `application.py` |
| FastAPI | `main.py` + `fastapi` import |

### 🌐 وب (JavaScript)

| نوع | فایل شاخص |
|-----|-----------|
| React | `src/App.jsx`, `src/App.tsx` |
| Vue | `vue.config.js`, `src/App.vue` |
| Angular | `angular.json` |
| Node.js | `package.json`, `server.js` |

### 🐘 PHP

| نوع | فایل شاخص |
|-----|-----------|
| Laravel | `artisan`, `composer.json` |
| Symfony | `symfony.lock` |
| WordPress | `wp-config.php` |

### ☕ سایر

| نوع | فایل شاخص |
|-----|-----------|
| Java/Spring | `pom.xml`, `build.gradle` |
| .NET | `*.csproj`, `*.sln` |
| Go | `go.mod` |
| Rust | `Cargo.toml` |

---

## 📄 انواع فایل‌های قابل شناسایی

### فایل‌های کد

| پسوند | زبان |
|-------|------|
| `.py` | Python |
| `.js`, `.jsx` | JavaScript |
| `.ts`, `.tsx` | TypeScript |
| `.php` | PHP |
| `.java` | Java |
| `.go` | Go |
| `.rs` | Rust |
| `.rb` | Ruby |
| `.c`, `.cpp`, `.h` | C/C++ |

### فایل‌های داده

| پسوند | نوع |
|-------|-----|
| `.json` | داده JSON |
| `.yaml`, `.yml` | تنظیمات YAML |
| `.xml` | داده XML |
| `.csv` | جدول |
| `.sql` | پایگاه داده |

### فایل‌های مستندات

| پسوند | نوع |
|-------|-----|
| `.md` | Markdown |
| `.txt` | متن ساده |
| `.html` | صفحه وب |
| `.pdf` | PDF |

### فایل‌های باینری (غیرمتنی)

| پسوند | نوع |
|-------|-----|
| `.png`, `.jpg` | تصویر |
| `.mp3`, `.wav` | صدا |
| `.mp4`, `.avi` | ویدیو |
| `.zip`, `.tar` | فشرده |
| `.exe`, `.dll` | اجرایی |

---

# 🔗 بخش نهم: ارتباط بین بخش‌ها

## نمودار ارتباطات

</div>

```
┌─────────────────────────────────────────────────────────────────┐
│                         کاربر                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌───────────────┐                         ┌───────────────┐
│    CLI        │                         │    GUI        │
│  (خط فرمان)   │                         │ (گرافیکی)    │
└───────────────┘                         └───────────────┘
        │                                           │
        └─────────────────────┬─────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────────┐
        │              ماژول‌های اصلی                  │
        ├─────────────────────────────────────────────┤
        │  Parser ──► Validator ──► Builder           │
        │     │                        │              │
        │     ▼                        ▼              │
        │  Scanner ──► ContentExtractor ──► Exporter  │
        │     │            │               │          │
        │     ▼            ▼               ▼          │
        │  Detector    Security       FileAnalyzer    │
        └─────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────────┐
        │              خدمات کمکی                     │
        ├─────────────────────────────────────────────┤
        │  Logger    Config    Utils    Cache         │
        └─────────────────────────────────────────────┘
```

<div dir="rtl">

## چگونه با هم کار می‌کنند؟

### سناریو ۱: ساخت پروژه

</div>

```
کاربر متن وارد می‌کند
        │
        ▼
    [Parser]
   تجزیه متن
        │
        ▼
   [Validator]
 بررسی صحت
        │
        ▼
   [Builder]
ساخت فایل‌ها
        │
        ▼
   فایل‌ها ساخته شدند ✅
```

<div dir="rtl">

### سناریو ۲: استخراج ساختار

</div>

```
کاربر پروژه را انتخاب می‌کند
        │
        ▼
   [Scanner]
 اسکن فایل‌ها
        │
        ▼
  [Detector]
تشخیص نوع پروژه
        │
        ▼
  [Exporter]
 صادر کردن
        │
        ▼
   خروجی آماده است ✅
```

<div dir="rtl">

---

# 💡 بخش دهم: نکات و ترفندها

## ۱. استفاده از dry-run
قبل از ساختن واقعی، همیشه با `--dry-run` پیش‌نمایش ببین!

## ۲. پشتیبان‌گیری
قبل از استفاده از `--force`، از فایل‌هایت پشتیبان بگیر!

## ۳. یادگیری Git
Git خیلی مهم است. یاد بگیر چگونه تغییراتت را ذخیره کنی.

## ۴. استفاده از پروفایل‌ها
برای کارهای تکراری، پروفایل بساز!

## ۵. خواندن لاگ‌ها
وقتی مشکلی پیش آمد، لاگ‌ها را بخوان!

---

# 🎓 کلام آخر

تبریک! 🎉

تو الان می‌دانی:
- ✅ StructureMaster چیست و چه کاری می‌کند
- ✅ هر ماژول چه وظیفه‌ای دارد
- ✅ چگونه از CLI و GUI استفاده کنی
- ✅ انواع پروژه‌ها و فایل‌ها را می‌شناسی
- ✅ بخش‌های مختلف چگونه با هم کار می‌کنند

**قدم بعدی چیست؟**

۱. برنامه را نصب کن
۲. با یک پروژه ساده تست کن
۳. کم‌کم ویژگی‌های بیشتر را امتحان کن
۴. اگر علاقه‌مندی، کد را بخوان و یاد بگیر!

---

<div align="center">

### 🌟 موفق باشی! 🌟

**ساخته شده با ❤️ برای جوانان علاقه‌مند به برنامه‌نویسی**

</div>

</div>
