# راهنمای کامل StructureMaster - به زبان فارسی 📘

## 🌟 معرفی

**StructureMaster** یک ابزار جامع، هوشمند و قابل گسترش برای تحلیل، ایجاد، استخراج و مستندسازی ساختار پروژه‌ها است. این نرم‌افزار به شما امکان می‌دهد:

- ساختار پروژه را از فایل‌های متنی، JSON یا کلیپ‌بورد بخوانید و بسازید
- ساختار پروژه‌های موجود را اسکن و استخراج کنید
- محتوای فایل‌ها را با متادیتای کامل استخراج کنید
- خروجی‌ها را در فرمت‌های مختلف با رمزنگاری صادر کنید

---

## 📋 فهرست مطالب

1. [نصب و راه‌اندازی](#-نصب-و-راهاندازی)
2. [رابط خط فرمان (CLI)](#-رابط-خط-فرمان-cli)
3. [رابط گرافیکی (GUI)](#-رابط-گرافیکی-gui)
4. [ویژگی‌های اصلی](#-ویژگیهای-اصلی)
5. [فرمت‌های ورودی](#-فرمتهای-ورودی)
6. [فرمت‌های خروجی](#-فرمتهای-خروجی)
7. [امنیت و رمزنگاری](#-امنیت-و-رمزنگاری)
8. [پروفایل‌ها](#-پروفایلها)
9. [تشخیص خودکار نوع پروژه](#-تشخیص-خودکار-نوع-پروژه)
10. [پرسش‌های متداول](#-پرسشهای-متداول)

---

## 🔧 نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیریت بسته‌های پایتون)

### نصب

```bash
# کلون کردن مخزن
git clone https://github.com/your-repo/structure-master.git
cd structure-master

# نصب وابستگی‌ها
pip install -r requirements.txt

# نصب بسته (اختیاری)
pip install -e .
```

### اجرا

```bash
# اجرای CLI
python -m src.cli.cli --help

# اجرای GUI
python -m src.main --gui

# یا با استفاده از entry points
structuremaster --help
structuremaster-gui
```

---

## 💻 رابط خط فرمان (CLI)

### دستورات اصلی

#### 1. اسکن پروژه (`scan`)

ساختار یک پروژه موجود را اسکن و استخراج می‌کند.

```bash
# اسکن ساده
structuremaster scan /path/to/project

# اسکن با خروجی فایل
structuremaster scan /path/to/project -o structure.json -f json

# اسکن با گزینه‌های بیشتر
structuremaster scan /path/to/project --include-hidden --no-recursive
```

**گزینه‌ها:**
| گزینه | توضیح |
|-------|--------|
| `-o, --output` | مسیر فایل خروجی |
| `-f, --format` | فرمت خروجی (json, txt, md, yaml, html) |
| `-h, --include-hidden` | شامل فایل‌های مخفی |
| `--no-recursive` | اسکن غیر بازگشتی |

---

#### 2. ساخت ساختار (`build`)

ساختار پروژه را از یک فایل ورودی می‌سازد.

```bash
# ساخت از فایل JSON
structuremaster build structure.json -o ./my-project

# ساخت با بازنویسی
structuremaster build structure.json -o ./my-project --force

# پیش‌نمایش بدون ایجاد
structuremaster build structure.json -o ./my-project --dry-run
```

**گزینه‌ها:**
| گزینه | توضیح |
|-------|--------|
| `-o, --output` | دایرکتوری خروجی |
| `-f, --force` | بازنویسی فایل‌های موجود |
| `-d, --dry-run` | پیش‌نمایش بدون ایجاد |

---

#### 3. استخراج محتوا (`extract`)

محتوای فایل‌ها را همراه با متادیتا استخراج می‌کند.

```bash
# استخراج ساختار فقط
structuremaster extract /path/to/project -o output.json

# استخراج با محتوا
structuremaster extract /path/to/project -o output.txt --content

# استخراج با رمزنگاری
structuremaster extract /path/to/project -o output.enc --encrypt -p mypassword
```

**گزینه‌ها:**
| گزینه | توضیح |
|-------|--------|
| `-o, --output` | مسیر فایل خروجی |
| `-c, --content` | استخراج محتوای فایل‌ها |
| `-f, --format` | فرمت خروجی |
| `-e, --encrypt` | رمزنگاری خروجی |
| `-p, --password` | رمز عبور رمزنگاری |
| `--profile` | استفاده از پروفایل |

---

#### 4. مقایسه (`compare`)

دو ساختار پروژه را مقایسه می‌کند.

```bash
structuremaster compare /path/to/old /path/to/new -o diff.md
```

---

#### 5. تحلیل کد (`analyze`)

متریک‌های کیفیت کد را تحلیل می‌کند.

```bash
structuremaster analyze /path/to/project -o analysis.json
```

---

#### 6. پیش‌نمایش (`preview`)

ساختار را از فایل یا کلیپ‌بورد پیش‌نمایش می‌کند.

```bash
# از فایل
structuremaster preview structure.json

# از کلیپ‌بورد
structuremaster preview --clipboard
```

---

## 🖥️ رابط گرافیکی (GUI)

### اجرای GUI

```bash
python -m src.main --gui
# یا
structuremaster-gui
```

### تب‌های اصلی

#### 1. 🔨 Structure Builder (سازنده ساختار)

در این تب می‌توانید:
- ساختار را به صورت متنی وارد کنید
- از کلیپ‌بورد بخوانید
- از فایل بارگذاری کنید
- پیش‌نمایش ساختار را ببینید
- ساختار را در مسیر دلخواه بسازید

**فرمت‌های پشتیبانی‌شده:**
- JSON: `{"src": {"main.py": null}}`
- Tree Text: `├── src/\n│   └── main.py`
- Plain Paths: `src/main.py`

---

#### 2. 📂 Extract Structure (استخراج ساختار)

در این تب می‌توانید:
- یک پروژه موجود را اسکن کنید
- نوع پروژه را به صورت خودکار تشخیص دهید
- آمار فایل‌ها و پوشه‌ها را مشاهده کنید
- ساختار را به فرمت‌های مختلف صادر کنید

---

#### 3. 📄 Extract Content (استخراج محتوا)

در این تب می‌توانید:
- محتوای تمام فایل‌ها را استخراج کنید
- متادیتای کامل شامل:
  - نام فایل و مسیر
  - اندازه
  - نوع MIME
  - هش SHA-256
  - تاریخ ایجاد و تغییر
  - کدگذاری
- خروجی را رمزنگاری کنید

---

#### 4. ⚙️ Settings (تنظیمات)

تنظیمات قابل پیکربندی:
- تم (تاریک/روشن)
- زبان
- دایرکتوری خروجی پیش‌فرض
- گزینه‌های اسکن
- گزینه‌های صادرات
- مدیریت پروفایل‌ها

---

## 🎯 ویژگی‌های اصلی

### ✅ بخش اول: سازنده ساختار
- خواندن ساختار از فرمت‌های مختلف
- اعتبارسنجی کامل (نام‌های معتبر، مسیرهای طولانی، تداخل‌ها)
- ایجاد فایل‌ها و پوشه‌ها
- پشتیبانی از `--force` و `--dry-run`
- پردازش موازی

### ✅ بخش دوم: استخراج ساختار
- اسکن بازگشتی پروژه‌ها
- تشخیص خودکار نوع پروژه
- نادیده گرفتن فایل‌های سیستمی
- کش هوشمند
- خروجی چندفرمتی

### ✅ بخش سوم: استخراج محتوا
- استخراج محتوای فایل‌ها
- تشخیص فایل‌های باینری
- متادیتای کامل (هش، MIME، تاریخ‌ها)
- رمزنگاری AES-256
- فشرده‌سازی

---

## 📥 فرمت‌های ورودی

### 1. JSON تودرتو
```json
{
  "src": {
    "main.py": null,
    "utils": {
      "helpers.py": null,
      "config.py": null
    }
  },
  "tests": {
    "test_main.py": null
  }
}
```

### 2. JSON فلت (لیست مسیرها)
```json
[
  "src/main.py",
  "src/utils/helpers.py",
  "src/utils/config.py",
  "tests/test_main.py"
]
```

### 3. متن درختی
```
├── src/
│   ├── main.py
│   └── utils/
│       ├── helpers.py
│       └── config.py
└── tests/
    └── test_main.py
```

### 4. مسیرهای ساده
```
src/main.py
src/utils/helpers.py
src/utils/config.py
tests/test_main.py
```

---

## 📤 فرمت‌های خروجی

| فرمت | پسوند | توضیح |
|------|-------|--------|
| JSON | `.json` | ساختاریافته، قابل پردازش |
| TXT | `.txt` | متن درختی خوانا |
| Markdown | `.md` | مناسب مستندات |
| YAML | `.yaml` | ساختاریافته، خوانا |
| HTML | `.html` | قابل نمایش در مرورگر |
| ZIP | `.zip` | آرشیو فشرده |
| TAR.GZ | `.tar.gz` | آرشیو فشرده |

---

## 🔐 امنیت و رمزنگاری

### رمزنگاری AES-256

StructureMaster از رمزنگاری AES-256-CBC با PBKDF2 برای مشتق‌سازی کلید استفاده می‌کند:

```bash
# رمزنگاری خروجی
structuremaster extract /project -o output.enc --encrypt -p mypassword
```

**مشخصات امنیتی:**
- الگوریتم: AES-256-CBC
- مشتق‌سازی کلید: PBKDF2 با SHA-256
- تکرار: 100,000 دور
- اندازه نمک: 16 بایت
- اندازه IV: 16 بایت
- HMAC برای تایید یکپارچگی

### تشخیص اطلاعات حساس

سیستم به صورت خودکار این موارد را تشخیص می‌دهد:
- کلیدهای API
- رمزهای عبور
- توکن‌های دسترسی
- کلیدهای AWS
- کلیدهای خصوصی
- URL‌های پایگاه‌داده

---

## 📋 پروفایل‌ها

پروفایل‌ها تنظیمات از پیش تعریف‌شده‌ای هستند که می‌توانید برای سناریوهای مختلف استفاده کنید.

### پروفایل‌های پیش‌فرض

| نام | توضیح |
|-----|--------|
| `full` | استخراج کامل با تمام متادیتا |
| `minimal` | فقط ساختار، بدون محتوا |
| `backup` | بهینه برای پشتیبان‌گیری (فشرده + رمزنگاری) |
| `security-audit` | متمرکز بر تشخیص اطلاعات حساس |

### استفاده از پروفایل

```bash
structuremaster extract /project -o output.json --profile full
```

### ایجاد پروفایل جدید

می‌توانید در تنظیمات GUI یا با ایجاد فایل در `profiles/` پروفایل جدید بسازید.

---

## 🔍 تشخیص خودکار نوع پروژه

StructureMaster به صورت خودکار انواع پروژه‌ها را تشخیص می‌دهد:

| نوع پروژه | فایل‌های شاخص |
|-----------|-----------------|
| Laravel | `artisan`, `composer.json`, `app/Http/Kernel.php` |
| React | `src/App.jsx`, `src/App.tsx`, `public/index.html` |
| Vue | `vue.config.js`, `src/App.vue` |
| Angular | `angular.json`, `src/app/app.module.ts` |
| Django | `manage.py`, `settings.py` |
| Flask | `app.py`, `application.py` |
| Node.js | `package.json`, `server.js` |
| .NET | `*.csproj`, `*.sln`, `Program.cs` |
| Spring | `pom.xml`, `application.properties` |
| Python | `setup.py`, `pyproject.toml`, `requirements.txt` |
| Go | `go.mod`, `main.go` |
| Rust | `Cargo.toml`, `src/main.rs` |

---

## ❓ پرسش‌های متداول

### چگونه فایل `.structureignore` کار می‌کند؟

مانند `.gitignore`، این فایل الگوهایی را تعریف می‌کند که در اسکن نادیده گرفته شوند:

```
# نادیده گرفتن node_modules
node_modules/

# نادیده گرفتن فایل‌های کامپایل‌شده
*.pyc
__pycache__/

# نادیده گرفتن فایل‌های محیطی
.env
.env.*
```

---

### چگونه از CLI با پروفایل استفاده کنم؟

```bash
structuremaster extract /my-project -o backup.zip --profile backup
```

---

### حداکثر اندازه فایل برای استخراج محتوا چقدر است؟

به صورت پیش‌فرض 100 مگابایت است، اما قابل تنظیم است:

```bash
# در CLI
# از طریق تنظیمات یا کد

# در GUI
# در تب Content Extractor > Max file size
```

---

### آیا می‌توانم از این ابزار به صورت کتابخانه استفاده کنم؟

بله! می‌توانید ماژول‌ها را مستقیماً import کنید:

```python
from src.modules.scanner import ProjectScanner
from src.modules.builder import StructureBuilder
from src.modules.exporter import Exporter

# اسکن پروژه
scanner = ProjectScanner()
result = scanner.scan("/path/to/project")

# ساخت ساختار
builder = StructureBuilder()
builder.build(result.structure, "/output/path")
```

---

## 📞 پشتیبانی

برای گزارش مشکلات یا پیشنهادات:
- GitHub Issues
- ایمیل: support@structuremaster.dev

---

## 📜 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

<div align="center">

**StructureMaster v1.0.0**

ساخته شده با ❤️

</div>
