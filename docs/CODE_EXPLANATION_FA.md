<div dir="rtl" align="right">

# 📖 توضیح کامل کدهای StructureMaster
## خط به خط — برای علاقه‌مندان به برنامه‌نویسی

---

<div align="center">

**🎯 هدف این راهنما:**

در این راهنما، هر خط از کدهای پروژه را بررسی می‌کنیم و توضیح می‌دهیم که چه کاری انجام می‌دهد.

این راهنما به تو کمک می‌کند تا بفهمی یک برنامه واقعی چگونه نوشته می‌شود!

نسخه: ۱.۰.۰ | آذر ۱۴۰۳

</div>

---

## 📋 فهرست فایل‌ها

| ردیف | فایل | وظیفه |
|-----:|------|-------|
| ۱ | config.py | تنظیمات برنامه |
| ۲ | logger.py | ثبت رویدادها |
| ۳ | parser.py | تجزیه ساختار |
| ۴ | validator.py | اعتبارسنجی |
| ۵ | scanner.py | اسکن پروژه |
| ۶ | builder.py | ساخت ساختار |
| ۷ | exporter.py | صادر کردن |
| ۸ | security.py | رمزنگاری |
| ۹ | content_extractor.py | استخراج محتوا |

---

# 🧠 مفاهیم پایه قبل از شروع

## 📦 import چیست؟

وقتی می‌خواهی از کدهایی که دیگران نوشته‌اند استفاده کنی، از `import` استفاده می‌کنی.

مثلاً: `import os` یعنی "کتابخانه os را بیاور تا بتوانم با فایل‌ها کار کنم"

## 🏠 class چیست؟

کلاس مثل یک **قالب** است. مثلاً اگر بخواهی ۱۰ تا ماشین بسازی، ابتدا یک نقشه (کلاس) می‌کشی.

مثال: `class Car:` یعنی "این نقشه ماشین است"

## ⚡ def چیست؟

`def` برای تعریف **تابع** (function) استفاده می‌شود. تابع مثل یک دستورالعمل است.

مثال: `def say_hello():` یعنی "این تابع سلام می‌گوید"

## 📝 متغیر (Variable) چیست؟

متغیر مثل یک **جعبه** است که چیزی را در آن نگه می‌داری.

مثال: `name = "Ali"` یعنی "در جعبه name نام علی را بگذار"

---

# 📄 فایل config.py — تنظیمات برنامه

**مسیر:** `src/config.py`
**خطوط:** ۴۱۲ خط
**وظیفه:** نگهداری تمام تنظیمات برنامه

## 🎯 این فایل چه کاری می‌کند؟

این فایل مثل **دفتر قوانین** برنامه است! همه تنظیمات مهم اینجا نوشته شده.

---

### خطوط ۱ تا ۴: توضیحات فایل

</div>

```python
"""
StructureMaster - Configuration Module
Contains all configuration settings, constants, and default values.
"""
```

<div dir="rtl">

> 💡 **توضیح:**
> این یک **docstring** است — یعنی توضیح درباره فایل.
> سه‌تا کوتیشن (`"""`) شروع و پایان توضیحات چندخطی را نشان می‌دهد.

---

### خطوط ۶ تا ۱۱: وارد کردن کتابخانه‌ها

</div>

```python
import os
from pathlib import Path
from typing import Dict, List, Set, Any, Optional
from dataclasses import dataclass, field
from enum import Enum, auto
import json
```

<div dir="rtl">

| خط | کتابخانه | کاربرد |
|---:|---------|--------|
| ۶ | `os` | کار با سیستم عامل (فایل‌ها، پوشه‌ها) |
| ۷ | `Path` | مدیریت آسان مسیر فایل‌ها |
| ۸ | `typing` | تعیین نوع متغیرها (مثلاً عدد، متن) |
| ۹ | `dataclass` | ساخت کلاس‌های ساده‌تر |
| ۱۰ | `Enum` | ساخت لیست گزینه‌های ثابت |
| ۱۱ | `json` | کار با فایل‌های JSON |

---

### خطوط ۱۴ تا ۲۱: تعریف سطوح لاگ

</div>

```python
class LogLevel(Enum):
    """Log levels for the application."""
    TRACE = 0      # جزئی‌ترین اطلاعات
    DEBUG = 1      # برای برنامه‌نویس‌ها  
    INFO = 2       # اطلاعات عادی
    WARN = 3       # هشدار
    ERROR = 4      # خطا
    CRITICAL = 5   # خطای خیلی مهم
```

<div dir="rtl">

> 💡 **توضیح:**
> **Enum** یعنی شمارشی — یک لیست از گزینه‌های ثابت!
> هر سطح یک عدد دارد. هرچه عدد بزرگتر باشد، پیام مهم‌تر است.

| سطح | عدد | معنی | مثال |
|-----|-----|------|------|
| TRACE | ۰ | جزئی‌ترین | "وارد تابع X شدم" |
| DEBUG | ۱ | اشکال‌زدایی | "مقدار x = 5" |
| INFO | ۲ | اطلاعات | "برنامه شروع شد" |
| WARN | ۳ | هشدار | "حافظه کم است" |
| ERROR | ۴ | خطا | "فایل پیدا نشد" |
| CRITICAL | ۵ | بحرانی | "برنامه کرش کرد" |

---

### خطوط ۲۴ تا ۴۱: انواع پروژه‌ها

</div>

```python
class ProjectType(Enum):
    """Supported project types for auto-detection."""
    UNKNOWN = auto()   # ناشناخته
    LARAVEL = auto()   # پروژه لاراول (PHP)
    REACT = auto()     # پروژه ری‌اکت (JavaScript)
    VUE = auto()       # پروژه ویو (JavaScript)
    ANGULAR = auto()   # پروژه انگولار (TypeScript)
    DJANGO = auto()    # پروژه جنگو (Python)
    # و سایر موارد...
```

<div dir="rtl">

> 💡 **توضیح:**
> - `auto()` یعنی "پایتون خودش عدد بده"
> - این کلاس انواع پروژه‌هایی که برنامه می‌شناسد را لیست می‌کند

---

### خطوط ۸۴ تا ۱۰۰: کلاس اصلی Config

</div>

```python
class Config:
    """Main configuration class for StructureMaster."""
    
    # Application Info
    APP_NAME = "StructureMaster"    # نام برنامه
    APP_VERSION = "1.0.0"           # نسخه برنامه
    
    # Default Settings
    DEFAULT_ENCODING = "utf-8"      # کدگذاری پیش‌فرض
    MAX_FILE_SIZE_MB = 100          # حداکثر اندازه فایل
    MAX_WORKERS = os.cpu_count()    # تعداد هسته‌های پردازنده
```

<div dir="rtl">

> 💡 **توضیح:**
> - `APP_NAME` — نام برنامه
> - `APP_VERSION` — نسخه برنامه (۱.۰.۰ یعنی نسخه اول)
> - **چرا حروف بزرگ؟** وقتی متغیری با حروف بزرگ می‌نویسیم، یعنی این یک **ثابت** است!

---

### خطوط ۱۱۷ تا ۱۴۲: پسوند فایل‌های باینری

</div>

```python
BINARY_EXTENSIONS: Set[str] = {
    # Images
    '.png', '.jpg', '.jpeg', '.gif', '.bmp',
    # Audio
    '.mp3', '.wav', '.ogg', '.flac',
    # Video
    '.mp4', '.avi', '.mov', '.mkv',
    # Archives
    '.zip', '.rar', '.7z', '.tar',
    # و موارد دیگر...
}
```

<div dir="rtl">

> 💡 **توضیح:**
> - **Set** یعنی مجموعه — لیستی که تکرار ندارد!
> - **فایل باینری چیست؟** فایلی که نمی‌توانی محتوایش را بخوانی (مثل عکس، ویدیو)

---

# 📄 فایل logger.py — سیستم ثبت رویدادها

**مسیر:** `src/modules/logger.py`
**خطوط:** ~۳۳۰ خط
**وظیفه:** ثبت اتفاقات برنامه

## 🎯 این فایل چه کاری می‌کند؟

این فایل مثل یک **دفتر خاطرات** عمل می‌کند! هر اتفاقی که در برنامه می‌افتد را یادداشت می‌کند.

---

### ساختار کلی Logger

</div>

```python
class Logger:
    """Multi-level logging system."""
    
    def __init__(self):
        """سازنده کلاس"""
        self.logs = []         # لیست همه لاگ‌ها
        self.level = LogLevel.INFO  # سطح پیش‌فرض
```

<div dir="rtl">

> 💡 **توضیح:**
> - `__init__` — تابع سازنده! هر وقت یک Logger جدید بسازی، این تابع اجرا می‌شود.
> - `self` — به خود شیء اشاره می‌کند. مثل "من" در جمله!
> - `self.logs = []` — یک لیست خالی می‌سازد

---

### توابع لاگ

</div>

```python
def info(self, message: str):
    """ثبت پیام اطلاعاتی - رنگ سبز"""
    self._log(LogLevel.INFO, message)

def warn(self, message: str):
    """ثبت پیام هشدار - رنگ زرد"""
    self._log(LogLevel.WARN, message)

def error(self, message: str):
    """ثبت پیام خطا - رنگ قرمز"""
    self._log(LogLevel.ERROR, message)
```

<div dir="rtl">

> 💡 **توضیح:**
> هر تابع یک نوع پیام را ثبت می‌کند:
> - `info()` — پیام اطلاعاتی (سبز)
> - `warn()` — هشدار (زرد)
> - `error()` — خطا (قرمز)

---

# 📄 فایل parser.py — تجزیه‌گر ساختار

**مسیر:** `src/modules/parser.py`
**خطوط:** ~۳۰۵ خط
**وظیفه:** فهمیدن ساختار از متن

## 🎯 این فایل چه کاری می‌کند؟

وقتی یک متن یا فایل به برنامه می‌دهی، این فایل آن را **می‌فهمد** و تبدیل به ساختار می‌کند!

---

### تابع parse — قلب تجزیه‌گر

</div>

```python
def parse(self, content: str) -> ParseResult:
    """تجزیه محتوا و تبدیل به ساختار."""
    
    # ابتدا تشخیص می‌دهیم چه نوع متنی است
    format_type = self._detect_format(content)
    
    if format_type == ParseFormat.JSON:
        return self._parse_json(content)
    elif format_type == ParseFormat.TREE:
        return self._parse_tree(content)
    else:
        return self._parse_plain(content)
```

<div dir="rtl">

| خط | توضیح |
|---:|-------|
| ۱ | تابع `parse` تعریف می‌شود. `content: str` یعنی ورودی باید متن باشد |
| ۴ | تشخیص می‌دهد متن JSON است یا درختی یا مسیرها |
| ۶-۷ | اگر JSON بود، از تابع `_parse_json` استفاده کن |
| ۸-۹ | اگر درختی بود، از تابع `_parse_tree` استفاده کن |
| ۱۰-۱۱ | در غیر این صورت، از تابع `_parse_plain` استفاده کن |

---

# 📄 فایل scanner.py — اسکنر پروژه

**مسیر:** `src/modules/scanner.py`
**خطوط:** ~۳۹۵ خط
**وظیفه:** پیدا کردن تمام فایل‌ها

## 🎯 این فایل چه کاری می‌کند؟

این فایل مثل یک **کاشف** عمل می‌کند! وارد پوشه می‌شود و همه فایل‌ها را پیدا می‌کند.

---

### تابع scan — اسکن پروژه

</div>

```python
def scan(self, path: Path, recursive: bool = True):
    """اسکن یک پوشه و استخراج ساختار."""
    
    for item in path.iterdir():      # روی همه آیتم‌ها بگرد
        if item.is_file():           # آیا فایل است؟
            self._process_file(item)
        elif item.is_dir() and recursive:  # آیا پوشه است؟
            self.scan(item, recursive)      # بازگشتی!
```

<div dir="rtl">

> 💡 **توضیح:**
> - **حلقه for:** روی همه آیتم‌های پوشه می‌گردد
> - `item.is_file()` — آیا این آیتم فایل است؟
> - `item.is_dir()` — آیا این آیتم پوشه است؟
> - **recursive** یعنی بازگشتی — اگر True باشد، وارد پوشه‌های داخلی هم می‌شود!
> - **خط آخر:** تابع خودش را صدا می‌زند! به این می‌گویند **توابع بازگشتی**.

---

# 📄 فایل builder.py — سازنده ساختار

**مسیر:** `src/modules/builder.py`
**خطوط:** ~۳۰۰ خط
**وظیفه:** ساختن فایل‌ها و پوشه‌ها

## 🎯 این فایل چه کاری می‌کند؟

این فایل مثل یک **کارگر ساختمانی** است! ساختار را می‌گیرد و فایل‌ها و پوشه‌ها را می‌سازد.

---

### تابع build — ساخت ساختار

</div>

```python
def build(self, structure: dict, output: Path):
    """ساخت ساختار در مسیر خروجی."""
    
    for name, content in structure.items():
        path = output / name         # مسیر جدید بساز
        
        if isinstance(content, dict):   # آیا پوشه است؟
            path.mkdir(parents=True, exist_ok=True)
            self.build(content, path)   # بازگشتی
        else:                            # فایل است
            path.touch()                 # فایل خالی بساز
```

<div dir="rtl">

> 💡 **توضیح:**
> - `structure.items()` — روی همه آیتم‌های دیکشنری می‌گردد
> - `output / name` — مسیر جدید می‌سازد (مثلاً /project/src)
> - `isinstance(content, dict)` — آیا محتوا یک دیکشنری است؟ اگر بله، پوشه است!
> - `mkdir()` — پوشه می‌سازد
> - `touch()` — فایل خالی می‌سازد

---

# 📄 فایل security.py — امنیت و رمزنگاری

**مسیر:** `src/modules/security.py`
**خطوط:** ~۳۶۰ خط
**وظیفه:** رمزنگاری اطلاعات

## 🎯 این فایل چه کاری می‌کند؟

این فایل اطلاعات را **رمزنگاری** می‌کند — مثل گذاشتن قفل روی یک صندوق!

---

### تابع encrypt — رمزنگاری

</div>

```python
def encrypt(self, data: str, password: str) -> bytes:
    """رمزنگاری داده با رمز عبور."""
    
    # ساخت نمک تصادفی
    salt = os.urandom(16)
    
    # ساخت کلید از رمز عبور
    key = self._derive_key(password, salt)
    
    # رمزنگاری با AES
    cipher = Cipher(algorithms.AES(key), modes.CBC(iv))
    encryptor = cipher.encryptor()
    
    # برگرداندن داده رمزنگاری شده
    return salt + iv + encryptor.update(padded_data)
```

<div dir="rtl">

> 💡 **توضیح:**
> - **Salt (نمک):** عدد تصادفی که امنیت را بیشتر می‌کند!
> - **Key (کلید):** از رمز عبور + نمک ساخته می‌شود
> - **AES-256:** الگوریتم رمزنگاری بسیار قوی

> ⚠️ **نکته مهم:**
> هرگز رمز عبور خود را فراموش نکن! اگر فراموش کنی، هیچ راهی برای بازگشت داده‌ها وجود ندارد!

---

# 🔗 ارتباط بین فایل‌ها

</div>

```
                      ┌─────────────────┐
                      │   کاربر         │
                      └────────┬────────┘
                               │
              ┌────────────────┴────────────────┐
              │                                 │
              ▼                                 ▼
       ┌─────────────┐                 ┌─────────────┐
       │    CLI      │                 │    GUI      │
       └──────┬──────┘                 └──────┬──────┘
              │                                │
              └────────────────┬───────────────┘
                               │
                               ▼
               ┌───────────────────────────────┐
               │        config.py              │
               │   (تنظیمات مرکزی)             │
               └───────────────┬───────────────┘
                               │
                               ▼
       ┌───────────────────────────────────────────────┐
       │                 ماژول‌ها                       │
       │                                               │
       │   parser.py ──────►  validator.py             │
       │        │                   │                  │
       │        ▼                   ▼                  │
       │   scanner.py ◄──── builder.py                 │
       │        │                   │                  │
       │        ▼                   ▼                  │
       │   content_extractor.py   exporter.py          │
       │        │                   │                  │
       │        └───────┬───────────┘                  │
       │                │                              │
       │                ▼                              │
       │          security.py                          │
       └───────────────────────────────────────────────┘
```

<div dir="rtl">

## چگونه با هم کار می‌کنند:

۱. **config.py** — همه ماژول‌ها از این فایل تنظیمات را می‌خوانند
۲. **parser** → **validator**: ابتدا ساختار خوانده می‌شود، سپس بررسی می‌شود
۳. **scanner** → **content_extractor**: ابتدا فایل‌ها پیدا می‌شوند، سپس محتوا خوانده می‌شود
۴. **exporter**: خروجی را در فرمت‌های مختلف ذخیره می‌کند
۵. **security**: در صورت نیاز، خروجی را رمزنگاری می‌کند

---

# 🎓 کلام آخر

تبریک! 🎉

تو الان فهمیدی که یک برنامه واقعی چگونه نوشته می‌شود!

- ✅ هر فایل یک وظیفه خاص دارد
- ✅ همه با هم کار می‌کنند
- ✅ کدها خوانا و مرتب هستند

**قدم بعدی:** خودت یک فایل ساده بنویس و امتحان کن!

---

<div align="center">

### 🌟 موفق باشی! 🌟

**ساخته شده با ❤️ برای جوانان علاقه‌مند به برنامه‌نویسی**

</div>

</div>
